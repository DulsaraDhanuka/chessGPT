mod tokenizer;

/*

Next step -
Parallely for each pgn
    - Grab pgn file
    - Loop through games
    - Tokenize
    - Write tokens to file

*/

fn main() {
    let tokenizer = tokenizer::Tokenizer::new();

    
}

